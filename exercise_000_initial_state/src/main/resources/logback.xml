<configuration>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>warn</level>
        </filter>
        <encoder>
            <pattern>%date{HH:mm:ss} %-5level %logger{0} [%X{akkaSource}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <file>${akka-streams.log-file:-akka-streams.log}</file>
        <encoder>
            <pattern>%date{HH:mm:ss} %-5level %logger{0} [%X{akkaSource}] - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.lightbend.demo.akkastreams" level="debug" additivity="false">
        <appender-ref ref="console"/>
        <appender-ref ref="file"/>
    </logger>

    <logger name="Remoting" level="off" additivity="false"/>
    <logger name="akka.remote.EndpointWriter" level="off" additivity="false"/>
    <logger name="akka.remote.ReliableDeliverySupervisor" level="off" additivity="false"/>

    <root level="warn">
        <appender-ref ref="console"/>
        <appender-ref ref="file"/>
    </root>

</configuration>
